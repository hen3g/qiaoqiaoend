# 2026-09-18

## 数据库定位与连通性确认
- 后端用 MySQL（mysql2/promise 连接池，见 `src/lib/db.ts`），连接信息取自 `.env.local` 的 `DATABASE_HOST/PORT/USER/PASSWORD/NAME`。
- 当前可连：主机 `182.254.140.32:3306`，库名 `qiaoqiaoend`，MySQL 5.7.44。实测连接正常。
- 全库 schema 集中在 `scripts/init-db.sql`（约 33 张表），`npm run db:init` 执行 `scripts/init-db.mjs` 灌库。
- `users` 表实测 3677 行。相关子表：`user_courses`、`user_skill_progress`、`user_course_summaries`、`email_bind_codes`、`password_reset_codes` 等。
- 注意：`.env.example` 里是旧项目 babyenglish 的配置（库名 `babyenglishs`），别连错环境。
- 排查经验：临时 mjs 脚本必须放在项目根目录运行，否则 ESM 解析不到 node_modules 里的 `mysql2`（NODE_PATH 对 ESM 无效）。
- 排查经验：自建连接默认没开 `namedPlaceholders`，写 `:xxx` 会报语法错误；项目内 `src/lib/db.ts` 的 pool 才开了命名占位符。临时脚本用 `?`。

## 运维操作：用户 wkzjy666 钻石清零
- 执行：`users.id=3944`（username=wkzjy666）diamonds 2596 → 0，事务内完成，同时补一笔 `diamond_transactions` 流水 `type='admin_adjust' amount=-2596`（保持账本与余额一致，`insertDiamondTransaction` 支持该类型）。
- 该库无触发器、无外键、无生成列，改 `users.diamonds` 不会级联。VIP 不受影响（仍到 2027-09-18）。
- 安全写法：事务 + `UPDATE ... WHERE id=? AND diamonds=?` + 校验 `affectedRows===1`，不符即回滚。
- 钻石相关逻辑集中在 `src/lib/diamond-transactions.ts`（类型枚举含 `admin_adjust`、账本分页查询默认只看近 30 天）。
